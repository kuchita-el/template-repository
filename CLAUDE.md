# CLAUDE.md (Project Scope)

このリポジトリでの Claude Code 作業ルール。ユーザースコープ（`~/.claude/CLAUDE.md`）の共通ルールに加え、本ファイルでプロジェクト固有事項を定義する。

> **雛形メモ**: 新規プロジェクトでは「プロジェクト概要」「技術スタック」「コマンド」を記入し、この行を削除する。

## プロジェクト概要

<!-- 何のプロジェクトか、1-3行で記入 -->

## 技術スタック

<!-- 言語・フレームワーク・主要ライブラリを記入 -->

## コマンド

<!-- ビルド・テスト・lint・型チェック等を記入。例:
- テスト: `...`
- lint: `...`
- 型チェック: `...`
-->

## 規約（共通）

詳細はユーザースコープ `~/.claude/CLAUDE.md` を正本とする。要点のみ再掲:

- **言語**: 応答・PR・コミットメッセージ・ドキュメントは日本語
- **コミット**: メッセージに HEREDOC を使わず `-m` を複数指定する
- **ブランチ**: 最新 `main` から `feature/{issue番号}-{簡潔な説明}` を作成。切替は `git switch`、ファイル復元は `git restore`
- **変更の進め方**: 既存の振る舞いを変更・削除する前に影響範囲を提示する。「単純化」名目で既存ケースを削除しない

## プロジェクト固有ルール

<!-- 上記共通規約への追加・上書きを記入 -->
